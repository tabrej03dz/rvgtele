@extends('layouts.crm',['title'=>'Sales Pipeline']) @section('content')@if(!$pipeline)<div class="bg-amber-50 p-5 rounded">No pipeline configured. Run the database seeder.</div>@else<div class="flex gap-4 overflow-x-auto pb-5">@foreach($pipeline->stages as $stage)<div class="min-w-72 bg-slate-200 rounded-xl"><div class="p-4 font-semibold flex justify-between"><span>{{ $stage->name }}</span><span>{{ ($leads[$stage->id]??collect())->count() }}</span></div><div class="p-3 space-y-3">@foreach($leads[$stage->id]??[] as $lead)<a href="{{ route('leads.show',$lead) }}" class="block bg-white rounded-lg p-4 shadow-sm"><b>{{ $lead->name }}</b><div class="text-sm text-slate-500">{{ $lead->company_name }}</div><div class="text-xs mt-2">₹{{ number_format($lead->expected_deal_value??0) }} · {{ $lead->assignedUser?->name }}</div></a>@endforeach</div></div>@endforeach</div>@endif@endsection