@extends('layouts.crm',['title'=>'Dashboard']) @section('content')<div class="grid md:grid-cols-4 gap-4 mb-8">@foreach([['Total Leads',$totalLeads],['New Today',$newToday],['Calls Today',$callsToday],['Connected',$connectedToday],['Due Follow-ups',$followUpsDue],['Overdue',$overdue],['Active Employees',$activeUsers],['Sales Value','₹'.number_format($sales,2)]] as $c)<div class="bg-white rounded-xl p-5 shadow-sm"><div class="text-sm text-slate-500">{{ $c[0] }}</div><div class="text-3xl font-bold mt-2">{{ $c[1] }}</div></div>@endforeach</div><div class="bg-white rounded-xl shadow-sm"><div class="p-5 border-b flex justify-between"><h2 class="font-semibold">Recent Leads</h2><a href="{{ route('leads.create') }}" class="bg-blue-600 text-white px-4 py-2 rounded-lg text-sm">+ Add Lead</a></div><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-3 text-left">Lead</th><th class="p-3">Mobile</th><th class="p-3">Status</th><th class="p-3">Owner</th></tr></thead><tbody>@forelse($recentLeads as $lead)<tr class="border-t"><td class="p-3"><a class="text-blue-600 font-medium" href="{{ route('leads.show',$lead) }}">{{ $lead->name }}</a></td><td class="p-3 text-center">{{ $lead->mobile }}</td><td class="p-3 text-center">{{ $lead->status?->name ?? 'New' }}</td><td class="p-3 text-center">{{ $lead->assignedUser?->name ?? 'Unassigned' }}</td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-slate-500">No leads yet.</td></tr>@endforelse</tbody></table></div></div>@endsection